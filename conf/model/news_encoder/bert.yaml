# @package _global_
defaults:
  - /tokenizer: bert

model:
  news_encoder:
    _target_: models.news_encoders.bert.BERTNewsEncoder
    pretrained_model_name: bert-base-uncased
    pooling_method: pooler
    finetune_n_last_layers: 2
