# @package _global_
defaults:
  - /tokenizer: nltk

model:
  _target_: models.NRMS.NRMS
  num_attention_heads: 15

news_encoder:
  _target_: models.NRMS.news_encoder.NewsEncoder
  word_embedding_dim: 300
  use_pretrained_embeddings: true
  freeze_pretrained_embeddings: false

features:
  - title
